/**
 * Shared constants for Deep Review feature to avoid duplication across modules.
 */

/** Regex matching the canonical /DeepReview slash command (case-sensitive). */
export const DEEP_REVIEW_COMMAND_RE = /^\/DeepReview(\s+.*)?$/;

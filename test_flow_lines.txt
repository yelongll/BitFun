proc 测试多层嵌套(): 整数 =
  # 测试多层嵌套流程线
  var 结果: 整数 = 0
  
  如果 条件1:
    结果 = 1
    当 计数 < 10:
      结果 = 结果 + 1
      如果 结果 > 5:
        结果 = 结果 * 2
      结束如果
    结束当
  结束如果
  
  对于 i 从 1 到 10:
    如果 i % 2 == 0:
      结果 = 结果 + i
    否则:
      结果 = 结果 - i
    结束如果
  结束对于
  
  返回 结果
